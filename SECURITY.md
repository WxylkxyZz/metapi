# Security Policy / 安全政策

**Canopy** 的安全性很重要。本自托管软件处理您的上游账号令牌与 API 代理流量，请务必妥善保管运行环境。

## 报告漏洞

如发现安全漏洞，请直接将详情发送至 GitHub Issues（私有仓库）或联系作者。

请勿在公开渠道披露权限提升、远程执行等敏感细节详情，直到作者确认修复。

## 部署注意事项

- 请务必修改默认的 `AUTH_TOKEN` / `PROXY_TOKEN`，并设置强随机的 `ACCOUNT_CREDENTIAL_SECRET`（未设置时服务会在启动日志中给出警告）。
- 默认 SQLite 数据库存储在 `DATA_DIR`（默认 `/app/data`），妥善备份该目录；使用 MySQL/Postgres 时同样做好备份。
- 不要在公网暴露 `127.0.0.1` 绑定的管理面板，除非您已启用 TLS 与访问控制。
- 启用「后台主动批量测活」前，请先确认你的上游中转站明确允许此类探测。

## 受支持的版本

单主分支持续迭代；建议及时同步最新提交以获取安全修复。目前没有长期支持（LTS）分支。